# 📚 دليل الاختبار - Testing Guide

## نظرة عامة

نظام اختبار شامل يغطي جميع جوانب المشروع من قاعدة البيانات إلى واجهة المستخدم.

---

## 🧪 أنواع الاختبارات

### 1. اختبارات قاعدة البيانات
```bash
npm run test:db
```
- التحقق من وجود الجداول
- التحقق من الأعمدة والقيود
- اختبار RLS والسياسات

### 2. اختبارات التكامل
```bash
npm run test:integration
```
- سيناريوهات المسح الكاملة
- منع التكرار والتجاوز
- حساب الإحصائيات

### 3. اختبارات End-to-End
```bash
npm run test:e2e
npm run test:e2e:ui  # مع واجهة رسومية
```
- تجربة المستخدم الكاملة
- اختبار PWA
- Responsive Design

---

## 🚀 البدء السريع

### التثبيت
```bash
npm install -D vitest jsdom @testing-library/react @testing-library/dom @testing-library/user-event @playwright/test @vitest/ui pg
npx playwright install
```

### الإعداد
```bash
cp .env.test .env
# أضف مفاتيح Supabase في .env
```

### التشغيل
```bash
npm test              # اختبار سريع
npm run test:all      # جميع الاختبارات
npm run test:coverage # تقرير التغطية
```

---

## 📋 قائمة الاختبار اليدوي

### Scanner Workflow
- [ ] فتح /scanner على الجوال
- [ ] السماح بأذونات الكاميرا
- [ ] مسح QR code صحيح
- [ ] اختيار عدد المرافقين
- [ ] التحقق من رسالة النجاح
- [ ] محاولة مسح نفس QR مرة أخرى (يجب أن يمنع)

### Client Dashboard
- [ ] تسجيل الدخول بكود الوصول
- [ ] عرض إحصائيات الحدث
- [ ] عرض آخر 10 عمليات مسح
- [ ] التحديث التلقائي كل 10 ثواني

### Invitation Studio
- [ ] اختيار حدث
- [ ] رفع ملف Excel
- [ ] رفع صورة خلفية
- [ ] تصميم في المحرر
- [ ] معاينة 3 دعوات
- [ ] تصدير ZIP

---

## 📊 التغطية المستهدفة

- **Statements:** > 70%
- **Branches:** > 60%
- **Functions:** > 70%
- **Lines:** > 70%

---

## 🔧 حل المشاكل

### الاختبارات لا تعمل
```bash
# تأكد من تثبيت المكتبات
npm install

# تأكد من ملف .env
cat .env
```

### Playwright يفشل
```bash
# أعد تثبيت المتصفحات
npx playwright install --force
```

---

لمزيد من التفاصيل، راجع ملفات الاختبار في مجلد `tests/`
